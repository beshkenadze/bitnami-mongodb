name: Build and Publish MongoDB Images

on:
  push:
    branches: [ "main" ]
    paths:
      - '6.0/**'
      - '8.0/**'
      - '.github/workflows/docker-build.yml'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-minideb:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: 'arm64,amd64'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build minideb:trixie base image
        id: build-minideb
        run: |
          cd 8.0
          docker build -t ghcr.io/${{ github.repository }}/minideb:trixie -f minideb-trixie-build.Dockerfile .
          docker run --rm --privileged ghcr.io/${{ github.repository }}/minideb:trixie | docker import - ghcr.io/${{ github.repository }}/minideb:trixie

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push minideb image
        if: github.event_name != 'pull_request'
        run: docker push ghcr.io/${{ github.repository }}/minideb:trixie

  build-mongodb:
    needs: build-minideb
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: ['6.0', '8.0']
        include:
          - version: '6.0'
            debian: 'debian-12'
          - version: '8.0'
            debian: 'debian-13'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: 'arm64,amd64'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/mongodb
          tags: |
            type=raw,value=${{ matrix.version }}
            type=raw,value=${{ matrix.version }}-${{ matrix.debian }}
            type=raw,value=latest,enable=${{ matrix.version == '8.0' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.version }}/${{ matrix.debian }}
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/mongodb:buildcache-${{ matrix.version }}
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/mongodb:buildcache-${{ matrix.version }},mode=max